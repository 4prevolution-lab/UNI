// Public runtime configuration. The protected runtime stays disabled by default.
// A Supabase anon key may be exposed here only after Row Level Security is verified.
window.UNI_RUNTIME_CONFIG = null;
